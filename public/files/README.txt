This folder contains downloadable files for the portfolio projects.

To add a file:
1. Place the PDF, ZIP, or Image file here.
2. Update the `files` array in `src/components/Projects.jsx` with the correct path (e.g., `/files/my-report.pdf`).

Example files are linked in the Projects component. Replace them with real files.
